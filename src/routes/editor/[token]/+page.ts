import { error } from '@sveltejs/kit'; import { editors, orders } from '$lib/data';
export const load=({params})=>{const editor=editors.find(e=>e.token===params.token);if(!editor)error(404,'This secure editor link is invalid or has expired.');const tasks=orders.flatMap(order=>order.tasks.filter(task=>task.assignee===editor.name).map(task=>({...task,orderId:order.id,project:order.project,customer:order.customer,workType:order.workType,fileLink:order.fileLink})));return{editor,tasks}};
