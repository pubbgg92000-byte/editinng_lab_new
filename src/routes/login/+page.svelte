<script lang="ts">import PortalHeader from '$lib/components/PortalHeader.svelte'; import { enhance } from '$app/forms'; import { ArrowRight, ShieldCheck } from 'lucide-svelte'; let {form}=$props(); let loading=$state(false);</script>
<svelte:head><title>Sign in — StudioFlow</title></svelte:head><PortalHeader/>
<main class="login"><div class="login-mark"><ShieldCheck size={19}/></div><h1>Welcome back</h1><p>Sign in to manage your studio’s editing work.</p><form method="POST" use:enhance={()=>{loading=true;return async({update})=>{await update();loading=false}}}><div class="field"><label for="email">Email</label><input id="email" name="email" type="email" value={form?.email||'admin@studioflow.app'} required/></div><div class="field"><label for="password">Password</label><input id="password" name="password" type="password" value="studioflow" required/></div>{#if form?.invalid}<div class="error">Email or password is incorrect.</div>{/if}<button class="primary" disabled={loading}>{loading?'Signing in...':'Sign in'} <ArrowRight size={14}/></button></form><span class="demo">Demo access is prefilled for this local MVP.</span></main>
<style>.login{width:min(380px,calc(100vw - 40px));margin:95px auto 0}.login-mark{width:38px;height:38px;border-radius:10px;background:#29223f;color:#b09aff;display:grid;place-items:center;margin-bottom:20px}.login h1{font-size:25px;letter-spacing:-.04em;margin:0 0 7px}.login>p{font-size:11px;color:#778294;margin:0 0 28px}.login form{display:flex;flex-direction:column;gap:17px}.login .primary{display:flex;align-items:center;justify-content:center;gap:7px;margin-top:3px}.error{color:#ef8585;font-size:10px}.demo{display:block;text-align:center;margin-top:18px;color:#515b6a;font-size:9px}</style>
